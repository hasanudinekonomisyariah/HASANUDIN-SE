import React from 'react';

/**
 * This component is no longer in use.
 * The teacher selection UI has been integrated directly into the
 * FingerprintScanner component for a more streamlined experience.
 */
const SelectTeacherModal: React.FC = () => {
  return null;
};

export default SelectTeacherModal;
